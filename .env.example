# ══════════════════════════════════════════════════════════════
#  Data Analizer Ops — template de entorno
#  Copiar a .env y completar. El .env va gitignoreado.
#  Todo el manejo de fechas es UTC (Greenwich). No hay timezone acá.
#
#  NADA LOCAL: la app no guarda data en disco. S3 se mira directo
#  (listados + muestras volátiles); lo persistente vive en Firebase.
# ══════════════════════════════════════════════════════════════

# ─── AWS / S3 (lectura de raw y bronze) ───────────────────────
# Usuario IAM de solo lectura sobre raw/*, bronze/v=1/* y schemas/*,
# más ce:GetCostAndUsage para la facturación.
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

S3_BUCKET=ingest-bucket-1985
# Prefijo de bronze (nombre histórico de la variable).
S3_PREFIX=bronze/v=1/
# Prefijo de raw.
S3_RAW_PREFIX=raw/v=1/

# Clave de partición por fecha (Hive): dt=YYYY-MM-DD -> "dt"
S3_DATE_PARTITION_KEY=dt

# Registro de contratos (labels y grupos de la vista Vivo).
S3_SCHEMA_PREFIX=schemas/

# ─── DuckDB (muestras directas de S3) ─────────────────────────
# Se instala con: winget install DuckDB.cli
# Vacío = se busca solo. La primera muestra instala la extensión
# httpfs (necesita internet una única vez).
DUCKDB_PATH=

# ─── Semáforo del ingestor ────────────────────────────────────
# Probe TCP cada 5 minutos: conectar = instancia levantada y escuchando.
# Vacíos = el ingest detrás de Caddy en el dominio, puerto 443.
INGEST_HOST=
INGEST_PORT=

# ─── Firebase (cuenta de servicio del Admin SDK) ──────────────
# Firestore: preferencias. Realtime Database: la vista Vivo.
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@TU-PROYECTO.iam.gserviceaccount.com
# En una línea, entre comillas, con los saltos de línea como \n:
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"
FIREBASE_DATABASE_URL=
